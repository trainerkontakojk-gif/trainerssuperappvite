# ── Environment ──
.env
.env.local
.env.local.old_project
.env.migration
.env.integration

# ── Dependencies ──
node_modules
.pnpm-store/

# ── Build Output ──
dist
.turbo
*.tsbuildinfo
apps/web/vite.config.js
apps/web/vite.config.d.ts

# ── Editor / OS ──
.vscode/
.commandcode/
.DS_Store
*.log

# ── Tooling / Config Agents ──
# Superpowers disabled: new output stays ignored; existing docs/superpowers specs/plans remain tracked as historical data.
.antigravitycli/
.kiro/
.opencode/
opencode.json
.superpowers
docs/superpowers/
# Legacy local plan archive. Canonical per-feature C/D plans live in plans/markdown/ (tracked).
/plan/*
!/plan/markdown/
/plan/markdown/*
!/plan/markdown/telefun-openai-webrtc-migration.md
.agents/
.codex/
skills-lock.json
.hermes/
.pi/

# ── Project-specific ──
apps/api/src/inspect_db.ts
apps/api/apply-migration.js
local-backups/
reference-repo
scratch/
supabase/.temp/
supabase/config.toml
supabase/.gitignore
.env.local.remote
remote_data_public.sql
supabase/seeds_backup/
# Laporan billing internal — jangan commit (berisi angka cost sensitif)
docs/ai-billing-credit-report-*.md

# ── Playwright / E2E ──
/test-results/
/playwright-report/
/blob-report/
playwright/.cache/
artifacts/
coverage/

# ── Graphify Knowledge Graph (local cache, not tracked) ──
graphify-out/
.herpes/
tsconfig.tsbuildinfo
